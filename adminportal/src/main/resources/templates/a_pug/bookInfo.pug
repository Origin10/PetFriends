//output:../bookInfo.html, doctype:strict
doctype html
html(lang='en', xmlns:th='http://www.w3.org/1000/xhtml')
  head(th:replace='common/header :: common-header')/
  body
    div(th:replace='common/header :: navbar')/
    .container
      div
        ol.breadcrumb
          li
            a(th:href='@{/book/bookList}')
              i.fa.fa-long-arrow-left(aria-hidden='true')
              | Go back
          li
            a(th:href='@{/book/updateBook?id=}+${book.id}') Edit
      .row
        .col-xs-3
          .panel.panel-default
            .panel-body
              img.img-responsive.center-block(th:src="@{/image/book/}+${book.id}+'.png'")
        .col-xs-9
          h3(th:text='${book.title}')
          .row
            .col-xs-6
              p
                strong Author: 
                span(th:text='${book.author}')
              p
                strong Publisher: 
                span(th:text='${book.publisher}')
              p
                strong Publication Date: 
                span(th:text='${book.publicationDate}')
              p
                strong Language: 
                span(th:text='${book.language}')
              p
                strong Category: 
                span(th:text='${book.category}')
              p
                strong Pages: 
                span(th:text='${book.numberOfPages}')
            .col-xs-6
              p
                strong Format: 
                span(th:text='${book.format}')
              p
                strong ISBN: 
                span(th:text='${book.isbn}')
              p
                strong Shipping Weight: 
                span(th:text='${book.shippingWeight}')
              p
                strong List Price: 
                span(th:text='${book.listPrice}')
              p
                strong Our Price: 
                span(th:text='${book.ourPrice}')
              p
                strong Number in Stock: 
                span(th:text='${book.inStockNumber}')
          p
            strong Description: 
            span(th:utext='${book.description}')
    div(th:replace='common/header :: body-bottom-scripts')/
